//auth.Controller 연결 통로

const express = require('express');
const router = express.Router();
const authController = require('../controller/auth/auth.controller')

// router.get('',loginController.showall);

module.exports = router();